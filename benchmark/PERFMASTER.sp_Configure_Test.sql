SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON

CREATE    PROCEDURE [PERFMASTER].[sp_Configure_Test]
	@TestClass				SYSNAME,
	@TestName				VARCHAR(255),
	@TestIterationName		VARCHAR(255)

AS
BEGIN
	SET NOCOUNT ON;
 

 -- test code to table 
 -- pull from there 
 -- run and execute function
 -- Test will include source & targaett

 --mat procs during runs
 -- to table and delete when done4

 end

GO
